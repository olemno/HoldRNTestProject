# HoldRNTestProject
Base project for React Native tests

## Set up
* Clone project && cd into folder
* run npm install
* Run project using react-native run-ios/android command

The project uses a common index called app.js for iOS and Android
